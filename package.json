{
  "name": "twitter-hp-monitor",
  "version": "1.0.0",
  "description": "Twitter HP Monitor Chrome Extension with TypeScript",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.0.0"
  },
  "private": true
}